<% provide(:title, 'Create event') %>
<% provide(:action, '/events') %>
<% provide(:button_text, 'Create event') %>
<div class="row justify-content-center jumbotron">
  <div class="col-md-6">
    <h1 class="text-center">Create event</h1>
    <%= render 'form' %>
  </div>
</div>