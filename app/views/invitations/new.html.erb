<% provide(:title, 'Invite user') %>
<% provide(:action, "/events/#{params[:id]}/invite") %>
<% provide(:button_text, 'Invite user') %>
<div class="row justify-content-center jumbotron">
  <div class="col-md-6">
    <h1 class="text-center">Invite user</h1>
    <%= render 'form' %>
  </div>
</div>